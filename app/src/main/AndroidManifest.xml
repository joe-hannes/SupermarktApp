<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dtg3.supermarktapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Angebote"></activity>
        <activity android:name=".RezeptdesTages" />
        <activity android:name=".Rezeptwelt" />
        <activity android:name=".Einkaufsliste" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ProductFinder"
            android:label="@string/title_activity_product_finder"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>